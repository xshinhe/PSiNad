set(ENV{PSINAD_INCLUDE_PATH} "@WRAPPER_INCLUDE_DIRS@")
set(ENV{PSINAD_LIB_PATH} "@CMAKE_INSTALL_PREFIX@/lib")
message("PSINAD_LIB_PATH = " $ENV{PSINAD_LIB_PATH})
message("PSINAD_INCLUDE_PATH = " $ENV{PSINAD_INCLUDE_PATH})
execute_process(
    COMMAND "pip install ."
    WORKING_DIRECTORY "@PYTHON_PACKAGE_DIR@"
)
